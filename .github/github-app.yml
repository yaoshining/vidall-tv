instructions: Use pnpm, not npm. Prefix branch names with feat/ or fix/. Run lint and format before committing
scripts:
  - name: Dep Install
    command: /Applications/DevEco-Studio.app/Contents/tools/ohpm/bin/ohpm install
    triggers: 
      - session.create
automation:
  auto_issue_session: true
  remote_control: false