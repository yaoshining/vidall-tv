## 1. 卡片交互分流

- [x] 1.1 恢复影视服务器"继续观看"宽卡为无悬浮遮罩、无中央播放按钮的整卡直播放样式，并保留现有进度条展示
- [x] 1.2 为"接下来"宽卡拆分卡片主体点击与中央播放按钮点击回调，确保主体仅进入详情、按钮仅触发直播放
- [x] 1.3 为"最近添加"海报卡拆分卡片主体点击与中央播放按钮点击回调，确保主体仅进入详情、按钮仅触发直播放
- [x] 1.4 调整中央播放按钮命中与焦点行为，使鼠标 hover 和遥控器焦点均可进入放大变色态，且焦点转移时遮罩不闪烁

## 2. 服务器直播放入口

- [x] 2.1 在媒体库首页收口统一的服务器直播放入口，校验当前服务器、条目 ID 和可播放性，失败时保持页面稳定且不误入详情页
- [x] 2.2 复用现有服务器 source adapter/播放源解析，将电影、单集及容器条目解析为具体可播放媒体并构造兼容的播放器参数
- [x] 2.3 将"继续观看"卡片、"接下来"播放按钮和"最近添加"播放按钮接入统一入口，同时保持两个分区的卡片主体详情导航不变

## 3. 续播与进度回显

- [x] 3.1 将服务端观看进度传入现有播放上下文，复用 playback progress service 的有效进度 seek 与 near-end 决策，不在首页重复判断
- [x] 3.2 验证首页直播放沿用约 10 秒周期、切后台和退出时的服务端进度上报，且上报失败不阻塞退出
- [x] 3.3 在首页直播放返回后触发一次服务器首页静默刷新，更新继续观看进度条、条目顺序及相关分区内容，并避免重复并发刷新

## 4. 质量验证

- [ ] 4.1 为卡片主体/播放按钮事件互斥、不可播放条目降级、有效进度续播和 near-end 行为补充或更新针对性测试
- [x] 4.2 运行仓库现有格式检查、静态检查及 ArkTS HAP 构建，确认无新增编译错误
- [x] 4.3 在已连接电视上安装当前 worktree 构建，分别使用鼠标和遥控器验证三类卡片行为、三态动效、直播放续播、后台/退出上报及返回进度回显，并截屏留存结果
