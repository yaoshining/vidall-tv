## 1. 服务器剧集上下文

- [x] 1.1 扩展播放上下文模型，新增显式类型的服务器上下文条目与导航能力，并通过类型检查确认不包含 `any`/`unknown` 或敏感认证字段
- [x] 1.2 实现服务器连续剧集上下文构建器，按季号、集号稳定排序并以 item ID 定位当前集；用单元测试验证乱序同季、多季跨季、末集和当前项缺失场景
- [x] 1.3 为 Jellyfin/Emby 与 Plex 接入完整季集数据获取和统一映射；用客户端映射测试或 mock 验证两类服务器均能生成相同结构

## 2. 播放入口接入

- [x] 2.1 在服务器剧集详情页播放「接下来」单集前构建上下文，验证导航参数包含定位到当前集的服务器播放上下文且失败时仍可单集播放
- [x] 2.2 在服务器季详情页的 Hero、跨季续播和集列表入口复用上下文构建器，验证任意入口均携带整剧连续上下文
- [x] 2.3 在服务器媒体库直播放入口识别剧集并附加上下文，验证电影与元数据不完整条目保持原播放参数

## 3. 自动下一集与切源

- [x] 3.1 扩展自动下一集管理器接受服务器上下文，验证倒计时、关闭设置、取消、末集和失败抑制测试与现有上下文行为一致
- [x] 3.2 实现服务器上下文目标项的播放器参数构建，切集时重新获取流地址、认证头和播放会话；用针对性测试验证目标 item ID 与流参数一致
- [x] 3.3 统一自动切换和选集面板的服务器切集路径，成功后更新上下文索引、标题、时长及服务端进度目标；验证失败时索引和当前播放源保持不变
- [x] 3.4 验证季末倒计时目标为下一季第一集，整剧最后一集不显示倒计时且不请求新播放源

## 4. 质量验证

- [x] 4.1 补齐 Jellyfin、Emby、Plex 的同季、跨季、退化和自动切换回归用例，并运行相关现有单元测试确认通过
- [x] 4.2 按仓库现有命令执行 format 与 lint，确认本变更文件无格式和 ArkTS 规则错误
- [x] 4.3 使用项目既有 HarmonyOS 验证链路执行针对性编译，确认日志包含 `BUILD SUCCESSFUL` 且退出码为 0
- [x] 4.4 核对 OpenSpec 任务与实现结果并运行严格校验，确认变更可实施但保持 `add-server-media-auto-next` 未归档
