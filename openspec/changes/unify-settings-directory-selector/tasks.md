## 1. 承接 #73 并定义统一契约

- [x] 1.1 盘点 `DirectorySelectorBuilder.ets` 与 `SmbDirectorySelectorBuilder.ets` 的重复状态、交互和差异点，形成共享目录选择状态机所需的字段与事件清单
- [x] 1.2 定义设置场景统一目录项模型与协议适配器接口，明确 WebDAV / SMB 的路径规范化、根目录文案和错误映射规则
- [x] 1.3 明确本 change 仅承接 `#73` 的后续设置场景统一，不把播放器文件浏览器或独立文件浏览页纳入实现范围

## 2. 抽离共享设置目录选择基础设施

- [x] 2.1 抽离统一设置目录选择 UI 容器，收口标题、返回按钮、面包屑、内容区、底部操作栏和别名命名弹窗
- [x] 2.2 实现共享目录选择状态机，覆盖初始化、加载、空态、错误态、保存中、根目录返回、多选、别名暂存与“全部文件夹”切换语义
- [x] 2.3 为共享状态机提供保存/取消/返回回调接口，使设置页装配层仅负责参数注入与场景退出

## 3. 接入 WebDAV / SMB 设置流程并保持兼容

- [x] 3.1 实现 WebDAV 设置目录选择适配器，过滤当前目录自引用项并保持现有路径拼接与已选目录回显兼容
- [x] 3.2 实现 SMB 设置目录选择适配器，保持共享列表根路径、目录路径、并发加载保护与错误提示兼容
- [x] 3.3 将 WebDAV / SMB 两个设置 Builder 改造为共享容器的薄装配层，同时保持现有 `SettingsController` 参数和 `file_source_directories` 存储语义不变

## 4. 回归验证与收尾

- [x] 4.1 验证 WebDAV / SMB 设置弹层中的面包屑、返回、加载态、空态、错误态、取消与完成行为一致
- [x] 4.2 验证多选目录、自定义别名、取消后重新勾选恢复别名以及“全部文件夹” root 标记语义不回退
- [x] 4.3 确认播放器文件浏览器、独立文件浏览页与文件级动作未被纳入本次实现或回归范围
