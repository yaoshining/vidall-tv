## 1. 来源契约与已有证据（#315）

以下勾选仅表示对应子分支里程碑已交付，不表示代码进入当前协调分支或Issue完成。记录截至2026-09-06；无归档任务，等待用户另行通知。

- [x] 1.1 统一来源身份、旧路由与真实删除语义；9bcb764及PR #321已有20项来源回归和真实删除链验证。
- [x] 1.2 补sourceLoaded响应式状态、resetAll入口恢复与主机TS解析兼容；a7906b7经独立复审，689ac376完成20 Hypium、2 resetAll及TS环境分支验证。
- [x] 1.3 验证目标TV来源切换与删除后UI刷新。2026-09-06用户明确确认#315已通过模拟器和真机验证，按用户验收确认接受；协调未亲跑，不声称已取得逐按键截图或原始设备日志。GitHub核实PR #321已合并main，合并提交d8882f66fce74d82717dab9e7ef32800dfdcd337，Issue #315已关闭，Project同步完成。此确认仅覆盖#315，不推广到#316或Epic集成验收。

## 2. 服务器适配与页面（#316，依赖#315）

- [x] 2.1 交付单实例三协议适配、身份校验、错误分类和100项有界首批；7c2765f的38项测试通过，彼时服务尚未进入生产编译图。
- [x] 2.2 接入真实文字、页面状态及搜索请求generation；c3559f72已通过55 Hypium及2 resetAll，独立构建成功且服务存在实际编译产物。历史竞态另见2.3，不视为页面全量验收。
- [x] 2.3 修复历史与关键词失效令牌混用；287c4f8已提交推送，独立QA通过55 Hypium、4页面、1真正reject/catch及2 resetAll，CompileArkTS实际执行成功，增量复审无阻断。页面覆盖历史加载、输入、返回后清空以及换源、离页和防抖失效；清空先于历史返回的精确顺序尚未直接覆盖，连同同来源历史写交叠及本地旧reject/finally页面测试纳入4.1补充。lint为SDK缺失uncheck，format为IDE单例阻塞，无真机证据，不视为全量验收。
- [ ] 2.4 接入具体服务器实例电影/剧集详情，测试正确媒体ID、类型、实例传递及不跳本地详情；420c8856f后发现的首次await身份、隐藏生命周期、冷入口及DB写入等待期间旧播放竞态，已由1f586f93e修复并提交推送。2026-09-06独立QA复验原update/delete等待探针均为pushes=[]、hasDetail=false，59 Hypium及host新增8组通过；本轮实际CompileArkTS 3.934秒、构建7.162秒成功，903文件hash一致。固定提交420..1f586增量复审无高置信新增缺陷，三文件及diff hash与QA冻结一致，身份隔离实现里程碑接受。修订守卫覆盖可见生命周期和播放每次await后/导航前，正常冷入口恰好请求一次，写失败、hide/show与旧finally已回归。此处保持未勾选：真实协议与TV详情返回闭环尚无证据，lint为SDK缺失uncheck、format为IDE单例阻塞；不能将实现里程碑接受等同完整验收。支持矩阵文档及真实验证前置核查已完成，详见2.6；真实环境操作待授权确认，不扩业务范围。
- [ ] 2.5 验证结果产生后实例删除、下线和返回重搜不串源，补自动回归与真实协议操作证据。2026-09-06固定638bd8d60只读盘点确认：已有真实删除模型、transport异常归一、session失效及页面局部方法测试，但均未覆盖非空结果后的完整搜索页面串联，不能据此验收或直接判定业务缺陷。已向原#316会话正式分发仅search_scope_test.cjs的三组补测：成功后真实删除并拒绝旧卡/跨源重搜；同实例网络失败清旧卡、同词恢复及迟到响应隔离；详情导航后leave/resume、执行防抖回调验证同词恰好一次重搜，并对照同协议不同实例的相同mediaId。工程冻结后独立QA；若暴露业务失败，先保留复现并审定最小修复范围。补测已由f4f9f8b1fbe82dddf738e2862f22525f01840931交付并推送，仅该测试文件变更；独立QA三链20/26/39共85个断言、59/59组合及现有选择器回归通过，各套件重叠不累加。协调固定638..f4增量复审未发现阻断，文件SHA256与QA冻结一致，接受自动回归子里程碑。真实页面方法、模型、session、service和客户端参与执行，DB/HTTP/nav与时钟仍为host边界；离页后无timer不能证明监听已移除，原生DB和真实ArkUI生命周期派发未验。此处保持未勾选，不替代真实HTTP/TV验证。
- [ ] 2.6 创建独立PR并记录三协议支持、首批限制和验证缺口；核验远端HEAD及检查，不自行合并关闭。文档子里程碑638bd8d60已验收，唯一变更为子分支的`.plans/plan-serverSearchSupportMatrix.md`，8个固定提交源码链接及diff检查通过，未创建PR。2026-09-06前置只读核查：devecocli发现1台已连接MateTV Pro，但安装许可、操作窗口和实际闭环未验；三协议授权实例与媒体样本来源尚未确认，未探测服务或读取凭据。文档TS绝对文件存在，命令与QA报告约定一致，原输出未回显完整命令，不声称逐字比对。设备存在不等于TV验收通过，保留真实验证及lint/format缺口。

## 3. 输入能力与共享框架（#317，依赖#315）

- [ ] 3.1 复核并复用本地拼音/服务器文字模式，测试能力切换不触发服务器本地拼音索引。
- [ ] 3.2 统一提交、清空、取消、返回和重入行为，验证两种输入复用结果与状态组件。
- [ ] 3.3 在TV验证输入区→结果区→详情→返回路径及焦点恢复，记录按键步骤后独立QA提交。

## 4. 异步焦点与性能（#318，同页串行）

- [ ] 4.1 审核本地和服务器全部请求生命周期，补乱序、同词重试、A→B→A、同ID配置修改和旧finally测试。
- [ ] 4.2 完善已有结果刷新、空态、错误重试呈现，验证异常不会统一变空列表。
- [ ] 4.3 优化较大结果集渲染及焦点恢复，在目标TV记录数据量、连续输入、滚动和返回可交互性，经独立QA提交。

## 5. 本地拼音相关度（#319，依赖#315）

- [ ] 5.1 建立中文、全拼、首字母、混合输入、多音词及噪声样例集，先记录当前命中和排序基线。
- [ ] 5.2 优化词段规范化和多音词召回，用样例验证漏召回修复且既有正确命中不退化。
- [ ] 5.3 调整短首字母边界并记录阈值依据，验证标题优先层级和服务器不受影响，经独立QA提交。

## 6. 来源历史与建议（#320，依赖#315）

- [ ] 6.1 核查旧历史归属并实现来源隔离迁移；测试来源与关键词联合身份、旧数据不误归服务器及事务失败回退。
- [ ] 6.2 接入本地片名补全、当前来源历史与示例；验证服务器无需在线补全且无AI/热搜/跨源内容。
- [ ] 6.3 验证切换来源后建议失效、选词仍在当前来源搜索以及焦点返回一致，经独立QA提交。

## 7. 集成验收与流程门禁

- [ ] 7.1 各阶段冻结后独立QA亲自执行现有runner和devecocli，保留包含新增文件的前后hash、完整命令及原始日志，记录lint/format真实结果而非仅退出码。2026-09-06工具诊断更新：当前安装SDK元数据为6.1.1/API24，与此前6.0.2/API22参数不匹配；匹配诊断参数后SDK定位成功，两份ETS目标的eslint扫描零defect，但arkPerfCheck存在TrapBuilder及Invalid ruleSet/name错误，综合lint仍未通过，也不替代API22基线验证；CJS被现有规则排除。formatter的-d支持无修改检查，但实际仍被共享IDE单实例门禁阻止，未扫描。诊断前后HEAD638bd8d60、904文件hash及clean状态一致；仅完成环境诊断，不关闭本门禁。原始证据保存在#316会话files/issue316-tooling-diagnostic，未修改SDK、项目配置或业务文件。
- [ ] 7.2 用户授权集成后安全合并所需分支；核验最终提交上的全链路回归、生产编译图、三协议及TV证据，不以子分支测试替代集成测试。
- [ ] 7.3 更新六项验收和Project状态，未解决环境/功能缺口逐项披露；Issue关闭交产品经理，Epic未验收不标完成。
