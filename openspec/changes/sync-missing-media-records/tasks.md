## 1. 同步契约与路径逻辑

- [x] 1.1 定义目标媒体请求、三态核验、目录组、进度和同步汇总的严格 ArkTS 类型，并通过项目类型检查确认无 `any`、`unknown` 或无类型对象字面量
- [x] 1.2 实现父目录提取、路径规范化及 `sourceId + directoryPath` 分组逻辑，并用单元测试验证中文、空格、尾斜杠、同目录去重和相似文件名精确匹配

## 2. 远程只读核验

- [x] 2.1 实现 WebDAV 只读目录核验适配器，仅将 HTTP 404 分类为明确不存在，并用单元测试验证成功列表、空列表、404、401 和连接错误
- [x] 2.2 实现 SMB 只读目录核验适配器，仅识别稳定的不存在结果，其余错误保守归为无法确认，并用单元测试固定支持的错误分类
- [x] 2.3 实现按目录组顺序核验并按视频数回调进度的协调逻辑，用单元测试验证每目录只访问一次、部分失败继续执行及不支持协议保留记录

## 3. 数据库安全清理

- [x] 3.1 在视频 DAO 中实现按安全批次删除指定视频 ID 的刮削关联、播放进度和视频记录，并通过数据库测试验证仅删除目标 ID 且可跨批次处理
- [x] 3.2 通过数据库门面公开孤立媒体清理能力，验证空集、空季、空剧和空电影按依赖顺序清理且非空层级保留
- [x] 3.3 组合批量视频清理与孤立层级清理，优先使用现有事务能力，并用失败路径测试或可观察错误结果验证不会把未完成清理报告为成功

## 4. 目标媒体同步服务

- [x] 4.1 实现电影同步入口，通过 `getVideosByMovieId` 仅核验电影关联路径并返回清理数、保留数、无法确认数和媒体是否仍存在，使用服务单元测试验证范围隔离
- [x] 4.2 实现电视剧同步入口，通过 `getVideosByTvSeriesId` 覆盖整剧所有季和集，并用服务单元测试验证从任意季触发仍核验整剧
- [x] 4.3 完成三态汇总与部分成功规则，用单元测试验证混合结果只删除明确不存在记录、全部无法确认时零删除、同目录其他媒体不受影响

## 5. 详情页交互接入

- [x] 5.1 抽取可复用的同步进度蒙层和结果文案，验证同步时显示“正在同步信息”“已检查 x/y”且阻止遥控器重复播放或同步
- [x] 5.2 为电影详情播放器入口补充 `PopInfo` 回调，接入“该电影可能已不存在”确认、同步、刷新或退出流程，并通过手工或组件测试验证取消不修改数据
- [x] 5.3 将季详情现有单集删除提示替换为整剧同步流程，并验证清空当前季但保留其他季时刷新页面、清空整剧时提示后退出
- [x] 5.4 将剧详情现有单集删除提示替换为整剧同步流程，并验证部分清理反馈包含清理数和无法确认数且目标仍存在时刷新
- [x] 5.5 验证三个详情页在全部无法确认或数据库清理失败时保留现有内容、解除阻塞状态并显示安全失败反馈

## 6. 回归与交付验证

- [ ] 6.1 补齐跨层集成测试，覆盖电影清空、空季收敛、整剧清空、部分网络失败、目录明确不存在、大批量删除和同目录多媒体隔离
- [x] 6.2 使用仓库现有命令执行格式化和 lint，确认所有新增 ArkTS 代码满足严格类型、`catch`、Promise 回调和 `build()` 语法约束
- [x] 6.3 执行最小相关单测构建与 HarmonyOS HAP 编译，确认日志出现 `BUILD SUCCESSFUL` 且播放器正常返回路径、现有手工删除和详情页返回行为无回归
