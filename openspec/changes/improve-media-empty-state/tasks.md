## 1. 状态与导航

- [x] 1.1 核对 `MediaLibraryModel` 的加载状态和媒体统计口径，确定“没有可展示内容”的统一判断，并通过现有字段组合验证不会把仅“最近添加”为空的非空媒体库误判为空态
- [x] 1.2 在媒体库组件中增加文件源与影视服务器设置导航方法，使用显式 `SettingsPageParam`，并验证两个目标类型分别为文件源设置和新增影视服务器

## 2. 空态界面

- [x] 2.1 重构媒体库渲染分支，使加载态、空态和非空内容互斥，并通过代码检查确认空态隐藏继续播放和媒体分区，同时保留底部统计、扫描及刮削区域
- [x] 2.2 更新空态标题和说明，增加“添加文件源”“添加影视服务器”两个 TV 可聚焦按钮，并验证焦点态样式与确认键导航行为
- [x] 2.3 保持非空文件源媒体库和影视服务器媒体库现有布局不变，并通过分支检查验证继续播放和内容分区仅在非空状态展示、统计扫描在空态和非空状态均可用

## 3. 验证

- [x] 3.1 运行 OpenSpec 严格校验，确认 proposal、spec、design 与实现任务一致且 change 保持未归档
- [x] 3.2 运行仓库已有格式化、lint 与 HarmonyOS 构建检查，确认 ArkTS 严格规则和 HAP 构建通过
- [x] 3.3 在电视上安装并启动当前 worktree 的 HAP，验证空态文案、区域隐藏、两个入口的遥控器焦点与导航，并记录目标设备和安装包路径
