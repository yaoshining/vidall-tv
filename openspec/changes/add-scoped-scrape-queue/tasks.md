## 1. 范围模型与查询基础

- [x] 1.1 定义电影、系列、季、单集、文件夹和全局范围 DTO，以及处理模式、候选策略、进度、暂停和结果类型；通过 ArkTS 类型检查确认不含 `any`、`unknown` 或未类型化对象字面量
- [x] 1.2 扩展视频与刮削关联 DAO，支持按电影关联、系列、系列与季号、视频 ID，以及“文件源 + 规范化目录边界”查询；用 DAO 单元测试验证 `/movie` 不包含 `/movie-old` 且相同路径的不同文件源彼此隔离
- [x] 1.3 实现统一范围解析器，将六类目标转换为稳定去重的视频集合；用单元测试覆盖单电影、整剧、多季、单季、单集、混合递归文件夹和全局范围
- [x] 1.4 集中实现 `incremental`、`fix-missing`、`overwrite` 三种模式的目标筛选判定，并与现有全局语义对齐；用参数化单元测试验证新增、指纹变化、关联缺失、海报缺失、完整元数据及覆盖全部目标

## 2. 统一刮削核心

- [x] 2.1 将 `VideoScrapeProcessor` 中分类、候选搜索、最佳候选选择、指定候选详情获取和持久化拆为可复用服务接口；通过现有扫描调用与新增服务测试确认自动刮削结果保持一致
- [x] 2.2 实现范围化刮削执行器，统一串联范围解析、模式筛选、媒体分组、候选策略、逐项写库和结果汇总；用服务测试验证局部与全局六类范围均调用同一执行路径
- [x] 2.3 实现电影与系列媒体分组及任务内系列候选缓存，按季号和集号应用分集详情；用测试验证同系列多集只搜索或确认一次、不同系列分别处理、混合文件夹分组正确
- [x] 2.4 将写库调整为“完整替代数据就绪后按单项目标提交”，禁止失败前清除旧关联；用故障注入测试验证搜索失败、详情失败、无候选、取消和持久化失败均保留旧元数据及范围外关联
- [x] 2.5 收敛 WebDAV、SMB 和媒体库页面中的重复模式判断与写库逻辑到统一核心；通过回归测试确认现有扫描结果和三种模式筛选行为不变

## 3. 异步任务队列与状态

- [x] 3.1 实现进程内单例 `ScrapeTaskQueue` 和只读可观察 `ScrapeTaskStore`，公开任务快照、派生活动数量及命令式操作；用单元测试验证 UI 无法直接修改内部任务对象
- [x] 3.2 实现安全钳制的有限并发、FIFO 调度和任务级进度更新；用可控异步执行器测试并发不超限、排队顺序稳定且入队立即返回
- [x] 3.3 实现等待候选确认暂停结果、续执行游标和恢复调度；用状态机测试验证等待确认释放并发槽、选择后保留进度重新排队且全局自动策略不会进入暂停态
- [x] 3.4 实现包含范围、规范目标、模式和候选策略的稳定活动键；用测试验证等价活动任务复用现有任务、同范围不同模式可并存、终态后可重新提交
- [x] 3.5 实现取消等待任务、取消候选组、失败或部分失败重试及终态清理；用测试验证运行中任务不可强制取消、重试仅包含失败或未成功目标并创建新记录
- [x] 3.6 实现有界终态历史与规定的状态优先级排序；用测试验证超限时优先淘汰最旧完成或取消记录，且不影响活动任务和失败记录的正常展示

## 4. 候选确认与设置

- [x] 4.1 在 `AppPreferences` 增加默认值为 0 的“局部刮削时选择匹配结果”偏好，并在“设置 → 资源库 → 元数据设置”提供开关及说明；通过偏好读写测试和设置页手动检查验证即时生效与默认关闭
- [x] 4.2 实现可复用的全局候选选择组件，展示标题、年份、海报和媒体类型，并覆盖加载、空结果、错误、重试和取消状态；通过组件/UI 测试验证各状态渲染与命令回传
- [x] 4.3 完成候选弹层的 TV D-Pad、OK、返回键及焦点恢复流程，并确保它与任务下拉不同时打开；通过遥控器 UI 测试验证无焦点陷阱和双弹层竞争
- [x] 4.4 将局部自动或确认策略及全局强制自动策略接入统一执行器；用测试验证设置关闭时采用最佳候选、开启时局部暂停确认、全局始终忽略设置

## 5. TopBar 任务入口

- [x] 5.1 实现可复用 `ScrapeTaskIndicator`，挂载到 Home TopBar 时钟左侧并由 Store 派生角标；通过 UI 测试验证角标只统计等待中与进行中任务，等待确认和终态不计数
- [x] 5.2 实现任务下拉组件，按规定优先级展示范围、目标名称、处理模式、状态、进度和结果摘要；通过组件测试验证排序、字段和空态
- [x] 5.3 根据任务状态接入取消、继续确认、重试和清除操作，并隐藏运行中任务的强制取消；通过 UI 测试验证每种状态只显示允许的操作
- [x] 5.4 完成任务图标与下拉列表的 D-Pad、OK、返回键焦点流；通过遥控器 UI 测试验证关闭列表后焦点稳定恢复到图标

## 6. 五类局部入口

- [x] 6.1 在电影详情 Hero“更多”菜单增加本地电影刮削入口，提供三种模式并为覆盖模式增加危险确认；通过页面测试验证影视服务器电影不显示入口且任务范围仅含当前电影
- [x] 6.2 在剧集详情 Hero“更多”菜单增加整剧刮削入口，提供三种模式并接入覆盖确认；通过页面测试验证任务包含该本地系列的全部本地分集并复用系列候选
- [x] 6.3 在季详情 Hero“更多”菜单增加整季刮削入口，提供三种模式并接入覆盖确认；通过页面测试验证任务仅包含当前系列当前季
- [x] 6.4 在季详情单集 `⋮` 菜单增加单集刮削入口，提供三种模式并接入覆盖确认；通过页面测试验证任务仅包含当前集且先确定系列再按季集号匹配
- [x] 6.5 在文件浏览器顶部工具栏增加递归文件夹刮削入口，模式选择后展示文件源、规范路径和预计视频数，并为覆盖模式增加危险确认；通过页面及范围集成测试验证全部子目录被包含且目录和来源边界正确
- [x] 6.6 统一五类入口的模式选择、覆盖确认和入队组件，确保页面不直接搜索候选或写库；通过代码检索和入口测试验证每类入口在三次遥控器按键内可达且只调用统一入队 API

## 7. 全局迁移与数据刷新

- [x] 7.1 将媒体库“新增和已修改”“缺少元数据”“覆盖”三个全局入口迁移到统一队列，保留覆盖危险确认并显式使用自动候选策略；通过集成测试验证全局任务不进入候选确认且页面不再维护重复写库流程
- [x] 7.2 实现按领域和目标标识发布的媒体数据版本或事件，任务成功提交后通知媒体库、电影详情、剧集详情和季详情刷新；通过 Store 测试验证相关页面刷新、无关页面不重复查询
- [x] 7.3 移除任务对页面实例和页面回调的引用，并在页面重新进入时比较数据版本；通过生命周期测试验证离开触发页后任务继续完成且再次进入可见新元数据
- [x] 7.4 执行局部与全局三种模式的回归矩阵，验证局部覆盖不影响范围外数据、失败保留旧数据、结果准确区分成功/失败/取消，并记录测试结论

## 8. 质量与交付验证

- [ ] 8.1 补齐范围解析、模式筛选、统一核心、队列状态机、去重、暂停、重试和数据刷新单元测试，并运行项目既有目标单测命令确认全部通过
- [ ] 8.2 补齐模式菜单、候选弹层、TopBar 任务列表和五类入口的 TV UI 测试，验证 D-Pad、OK、返回键、可见焦点及三次按键可达性
- [ ] 8.3 使用项目既有格式化和 lint 命令检查全部改动，确认 ArkTS 严格规则无新增告警
- [x] 8.4 按当前 worktree 的 HarmonyOS 构建流程执行目标 HAP 编译，且仅在 hvigor 退出码为 0 并出现 `BUILD SUCCESSFUL` 时标记通过
- [ ] 8.5 对多任务后台执行期间的页面导航、焦点移动和视频播放进行真机或模拟器回归，并验证 TopBar 数量、任务操作和跨页面刷新符合规格
- [x] 8.6 执行 OpenSpec verify 确认实现与 proposal、design、两份 delta spec 和任务清单一致；保留变更未归档，等待用户另行通知
