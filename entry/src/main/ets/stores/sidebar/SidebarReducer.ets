import { Action, Reducer } from "@hadss/state_store";
import { SidebarModel } from "./SidebarModel";

export const sidebarReducer: Reducer<SidebarModel> = (state: SidebarModel, action: Action) => {
  return null
}